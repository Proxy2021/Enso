{
  "version": 1,
  "spec": {
    "toolFamily": "browser_data",
    "toolPrefix": "enso_browser_data",
    "description": "Browser Data: analyze your Chrome/Edge browsing patterns — history, bookmarks, top domains, recent searches, and saved sites.",
    "signatureId": "browser_data_view",
    "tools": [
      {
        "suffix": "browse",
        "label": "Browse Browser Data",
        "description": "Browse your browser history and bookmarks — top domains, recent searches, saved bookmarks by folder.",
        "isPrimary": true,
        "parameters": {
          "type": "object",
          "properties": {
            "view": {
              "type": "string",
              "enum": ["history", "bookmarks"],
              "description": "Which view to show (default: history)"
            },
            "query": { "type": "string", "description": "Filter by domain, search term, or bookmark title" },
            "folder": { "type": "string", "description": "Filter bookmarks by folder name" }
          },
          "additionalProperties": false
        }
      },
      {
        "suffix": "scan_history",
        "label": "Scan Browser History",
        "description": "Scan Chrome/Edge browsing history from the local SQLite database.",
        "parameters": {
          "type": "object",
          "properties": {
            "browser": { "type": "string", "enum": ["chrome", "edge", "all"], "description": "Browser to scan (default: all)" },
            "sinceDays": { "type": "number", "description": "Days of history to include (default: 30)" }
          },
          "additionalProperties": false
        }
      },
      {
        "suffix": "scan_bookmarks",
        "label": "Scan Bookmarks",
        "description": "Scan Chrome/Edge bookmark files.",
        "parameters": { "type": "object", "properties": {}, "additionalProperties": false }
      },
      {
        "suffix": "update",
        "label": "Update Browser Data",
        "description": "Incremental scan — check for new browsing history and bookmarks since last scan and update the Cortex with any changes.",
        "parameters": { "type": "object", "properties": {}, "required": [], "additionalProperties": false }
      }
    ]
  },
  "createdAt": 1775460000000
}
